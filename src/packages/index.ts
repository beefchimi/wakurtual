// The `src/packages` folder is meant to be treated as if each folder is an NPM package.
// Therefor, we do not export the ocntents of each folder from this barrel file.

export const contentiousVersion = '0.0.1';
export const hooksVersion = '0.0.1';
export const typesVersion = '0.0.1';
export const utilitiesVersion = '0.0.1';
